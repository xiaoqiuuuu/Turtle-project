# 实现模板创作的功能

from animation import increase_window_size
from Myturtle import Myturtle

class MyProgram:






def template(window, t):
    t = Myturtle(screen)
    # 绘制党旗红色背景
    # 设置画笔颜色为红色，后期填充也是红色
    try:
        t.color("white")
    except:
        print(a)
        del t
        return

    # 左边是一个frame
    add_Keyframe(window, t)
    increase_window_size(window, 20, 1200, 0)
    welcome(t)



